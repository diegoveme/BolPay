# Backend environment (NestJS + Prisma). Copy to apps/backend/.env for local dev.
PORT=3000
NODE_ENV=development

# PostgreSQL (Supabase). Pooled (PgBouncer, 6543) for the app; direct (5432) for migrations.
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@<pooler-host>:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@<pooler-host>:5432/postgres"

# Trustless Work (escrow) - Testnet during development.
TRUSTLESS_WORK_API_URL="https://dev.api.trustlesswork.com"
TRUSTLESS_WORK_API_KEY=""

# Stellar - USDC is a native Stellar asset (issuer below); no third-party
# payments provider is required.
STELLAR_NETWORK="testnet"
USDC_ISSUER="GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5"

# Auth
JWT_SECRET="change-me-in-local-env"
JWT_EXPIRES_IN="1d"

# SMTP (transactional email: email verification + invitations).
# Leave empty to log links to the console instead of sending (local dev).
# Gmail: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_USER=your@gmail.com,
# SMTP_PASS=<Google App Password> (needs 2-Step Verification enabled).
SMTP_HOST=""
SMTP_PORT="465"
SMTP_USER=""
SMTP_PASS=""
MAIL_FROM="BolPay <your@gmail.com>"
# Public URL of the web client (used to build links in emails).
WEB_URL="http://localhost:5173"

# Supabase Storage for avatars/logos. URL + anon key (the avatars bucket is
# public-read with an anon insert policy). Leave empty to disable file uploads.
SUPABASE_URL=""
SUPABASE_ANON_KEY=""
