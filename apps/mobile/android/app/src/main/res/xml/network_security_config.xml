<?xml version="1.0" encoding="utf-8"?>
<!--
    Fail-closed cleartext policy: HTTP is rejected everywhere by default so a
    misconfigured release can never send tokens over plaintext. The only
    exception is the emulator loopback (10.0.2.2) and localhost used during
    local development against the backend running on the host machine.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
